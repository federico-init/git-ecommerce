// import css
import "./Navbar.css";

const Navbar = () => {
  return (
    <nav>
      <div>
        <a href="#">About Us</a>
        <a href="#">Collection</a>
        <a href="#">Contacts</a>
      </div>
      <div>
        <p>Search</p>
        <p>Cart</p>
      </div>
    </nav>
  );
};

export default Navbar;
